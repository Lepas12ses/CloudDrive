{
	"name": "cloud-drive",
	"version": "1.0.0",
	"scripts": {
		"dev": "npx concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=client\"",
		"lint": "npm run lint --workspace=client",
		"prepare": "husky"
	},
	"workspaces": [
		"backend",
		"client"
	],
	"devDependencies": {
		"concurrently": "^9.2.1",
		"husky": "^9.1.7"
	}
}
